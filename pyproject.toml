[project]
name = "llm-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tom Rochette", email = "roctom@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "llm-ollama>=0.11",
    "llm>=0.26",
]

[project.scripts]
llm-server = "llm_server.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
